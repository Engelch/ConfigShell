#! /usr/bin/env fish

pwd
cd Output00DIR
pwd
cd . | read a
echo a is $a
cd ..
if test "$a" = '123_test test'
    echo ok: passed (dirname (pwd) | xargs basename)
else
    echo FAIL: test (dirname (pwd) | xargs basename):output was $a
end

# eof
