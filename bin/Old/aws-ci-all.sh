#!/usr/bin/env bash

$(dirname $0)/aws-ci-build.sh $@ && \
   echo ================================================ &&\
   echo ================================================ &&\
   echo aws-ci-build was successful &&\
   echo ================================================ &&\
   echo ================================================ &&\
   $(dirname $0)/aws-ci-push.sh $@
