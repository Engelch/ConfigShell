#!/usr/bin/env bash

$(dirname $0)/ci-build.sh $@ && \
   echo ================================================ &&\
   echo ================================================ &&\
   echo ci-build was successful &&\
   echo ================================================ &&\
   echo ================================================ &&\
   $(dirname $0)/ci-push.sh $@
