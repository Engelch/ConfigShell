[ ! -f "$1" ] && echo 1st argument not found as a file > /dev/stderr && exit 1
outfile=$(basename "$1" .p7b).pem
[ -f "$outfile" ] && echo output file already existing > /dev/stderr && exit 2
openssl pkcs7 -inform DER -outform PEM -print_certs -in "$1" -out "$outfile"
